# AI-Native-Infrastructure-Manager
This project bridges the gap between human intent and complex configuration management. By leveraging local LLMs, it allows users to modify infrastructure settings (like memory, replicas, and env variables) using plain English, ensuring every change is validated against strict industry-standard schemas.
